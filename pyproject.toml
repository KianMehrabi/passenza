[project]
name = "passenza"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "KianMehrabi", email = "kian.mehrabi123@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
passenza = "passenza:main"

[build-system]
requires = ["uv_build>=0.12.16,<0.13.0"]
build-backend = "uv_build"
